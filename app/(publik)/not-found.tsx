import { GmniNotFound } from "@/components/ui/GmniNotFound";

/** 404 area publik (fallback konsisten dengan root). */
export default function PublikNotFound() {
  return <GmniNotFound area="publik" />;
}
