import { GmniNotFound } from "@/components/ui/GmniNotFound";

/**
 * Halaman 404 — memakai motif banteng/maskot & Trisila (Bagian 3).
 */
export default function NotFoundPage() {
  return <GmniNotFound area="publik" />;
}
